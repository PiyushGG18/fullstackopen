{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "build:ui": "rm -rf dist && cd ../../2_React2/part2 && npm run build && cp -r dist ../../3_Backend/part3",
    "deploy:full": "npm run build:ui && cd ../../ && git add . && git commit -m uibuild && git push",
    "start": "node index.js",
    "dev": "nodemon --legacy-watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^5.2.1",
    "nodemon": "^3.1.14"
  }
}
